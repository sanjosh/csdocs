
columnar?
expressions
encoders - 
codegen
unsafe vs safe

In Plan nodes
{
	output and input partitions 
	requiredChildDistribution
}

---

what modules do u keep in mind when making changes
what is typical workflow
testing

---

hibernate spring tableau - auto generate complex sql queries

GNU trove - java hash map THashMap

StoreDataSourceStrategy.scala in snappydata
See Literal - ParamLiteral- CodeGen::init

EnsureRequirements.scala

oracle has subpartitions 

use of Flight recorder, mission control

jvm only compiles part of method based on likely branches, may decompile and recompile if other branch hit
copying one byte at a time from off-heap memory via JNI is expensive
